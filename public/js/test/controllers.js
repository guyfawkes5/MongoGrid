describe('controllers', function() {
    beforeEach(module('MongoUI'));

    beforeEach();

    describe('mongoController', function() {
        it('should broadcast a schema load when clicked', function (_$controller_) {
            var scope = {},
                ctrl = _$controller_('mongoController', {$scope: scope});


        });
    });
});